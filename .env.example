# Nano Banana API Configuration
# Get your API credentials from nano banana dashboard
NANO_BANANA_API_ENDPOINT=https://api.nanobanana.com/v1/generate
NANO_BANANA_API_KEY=your_nano_banana_api_key_here

# Optional: Custom settings
NANO_BANANA_DEFAULT_STYLE=modern
NANO_BANANA_DEFAULT_QUALITY=high
NANO_BANANA_TIMEOUT=30000

# Clerk Authentication
# Get your publishable key from https://clerk.com/dashboard
VITE_CLERK_PUBLISHABLE_KEY=pk_test_your_key_here

# Convex Backend
# Get your URL from https://dashboard.convex.dev
VITE_CONVEX_URL=https://your-project.convex.cloud

# Sanity.io CMS
# Project ID from https://sanity.io/manage -> Project -> Settings -> API
VITE_SANITY_PROJECT_ID=55u2jb6b
VITE_SANITY_DATASET=production
# Required for the public site to read non-singleton docs (blog posts, programs,
# team members, etc.). Sanity Growth-tier datasets gate anonymous reads on most
# doc types — a Viewer-only token bypasses this.
# Generate at sanity.io/manage -> BBB New Site -> API -> Tokens (Viewer permission).
# Token is bundled into the client — MUST be Viewer (read-only), never Editor/Admin.
VITE_SANITY_API_TOKEN=